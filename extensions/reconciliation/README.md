# OperaLa Reconciliation Extension

This directory contains the built-in reconciliation capability planning and
runtime templates for OperaLa.

The current implementation is provided by the main `greentic-operala` crate and
uses this directory as the stable extension home. Future PRs may split the
implementation into a dedicated crate under this directory once the API settles.
