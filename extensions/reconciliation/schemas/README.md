# Reconciliation Schemas

Repository-level runtime schemas live under `schemas/`. Runtime schema
generation can move capability-specific fragments here when the reconciliation
extension becomes a separate crate.
